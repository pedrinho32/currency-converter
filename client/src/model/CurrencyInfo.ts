export interface CurrencyInfo {
    country: string;
    currency: string;
    amount: number;
    code: string;
    rate: number;
}


export interface HeaderInfo  {
    country: string;
    currency: string;
    amount: string;
    code: string;
    rate: string;
}